import React from "react";
import { Heading } from "@chakra-ui/react";

export const EventPage = ({
  id,
  createdBy,
  title,
  description,
  image,
  categoryIds,
  attendedBy,
  location,
  startTime,
  endTime,
}) => {
  return <Heading>EventPage</Heading>;
};
